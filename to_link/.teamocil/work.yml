session:
  name: "Work"
  windows:
    - name: "cfg"
      root: "~/.dotfiles"
      layout: "e3dd,181x45,0,0[181x35,0,0,2,181x9,0,36,3]"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/config"
          focus: true
        - cmd: "git status"
    - name: "hrh"
      root: "~/Sites/acp-hotrodhotline"
      layout: "main-horizontal"
      filters:
        before: "cd ~/Sites/acp-hotrodhotline; vagrant up"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/hrh"
    - name: "acp"
      root: "~/Sites/auto-classifieds-platform"
      layout: "main-horizontal"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/acp"
          focus: true
    - name: "vm"
      root: "~/Sites/acp-hotrodhotline"
      layout: "main-horizontal"
      filters:
        before: "cd ~/Sites/acp-hotrodhotline; vagrant up"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/vm"
          focus: true
        - cmd: "vagrant ssh"
    # - name: "bustersphp"
      # root: "~/Sites/bustersphp"
      # layout: "main-horizontal"
      # clear: true
      # panes:
        # - cmd: "vim -S ~/.vim/sessions/busters"
          # focus: true
    # - name: "gitolite"
      # root: "~/Sites/gitolite-admin"
      # layout: "main-horizontal"
      # clear: true
      # panes:
        # - cmd: "vim -S ~/.vim/sessions/gitolite"
          # focus: true
        # - cmd: "cd keydir"
    # - name: "rj"
      # root: "~/Sites/racingjunk"
      # clear: true
      # layout: "e3dd,181x45,0,0[181x35,0,0,2,181x9,0,36,3]"
      # panes:
        # - cmd: "vim -S ~/.vim/sessions/rj"
          # focus: true
        # - cmd: "git status"
    # - name: "rj-vm"
      # root: "~/vms/mikes_vagrant_box"
      # layout: "main-horizontal"
      # filters:
          # before: "cd ~/vms/rjvagrant; vagrant up"
      # clear: true
      # panes:
        # - cmd: "vagrant ssh -c 'tail -f /var/log/apache2/*log /var/log/mysql/*log /var/log/mysql.err /var/log/mysql.log'"
        # - cmd: "vim files/rm.sql"
        # - cmd: "vagrant ssh"
    # - name: "vpn"
      # root: "~"
      # panes:
        # - cmd: "vpn"
