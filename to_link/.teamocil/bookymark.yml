
session:
  name: "Bookymark"
  windows:
    - name: "cfg"
      root: "~/.dotfiles"
      layout: "e3dd,181x45,0,0[181x35,0,0,2,181x9,0,36,3]"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/config"
          focus: true
        - cmd: "git status"
    - name: "bookymark"
      root: "~/Sites/bookymark.com"
      layout: "main-horizontal"
      clear: true
      panes:
        - cmd: "vim -S ~/.vim/sessions/bookymark"
          # focus: true
        # - cmd: "vagrant ssh"
    # - name: "rj"
      # root: "~/Sites/racingjunk"
      # clear: true
      # layout: "e3dd,181x45,0,0[181x35,0,0,2,181x9,0,36,3]"
      # panes:
        # - cmd: "vim -S ~/.vim/sessions/rj"
          # focus: true
        # - cmd: "git status"
    # - name: "rj-vm"
      # root: "~/vms/mikes_vagrant_box"
      # layout: "main-horizontal"
      # filters:
          # before: "cd ~/vms/rjvagrant; vagrant up"
      # clear: true
      # panes:
        # - cmd: "vagrant ssh -c 'tail -f /var/log/apache2/*log /var/log/mysql/*log /var/log/mysql.err /var/log/mysql.log'"
        # - cmd: "vim files/rm.sql"
        # - cmd: "vagrant ssh"
    - name: "vm"
      root: "~/vms/Vaprobash"
      layout: "even-vertical"
      filters:
          before: "cd ~/vms/Vaprobash; vagrant up"
      clear: true
      panes:
        - cmd: "vagrant ssh"
        - cmd: "vim Vagrantfile"
    # - name: "vpn"
      # root: "~"
      # panes:
        # - cmd: "vpn"
